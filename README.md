# THE MONITORING GALLERY

A map — drawn from the pilot seat — of everything wired *around* an LLM forward pass: the
interpretability lenses that read the residual stream (**J-space**) through one-way glass, the
external knobs that steer it, the baked-in RLHF/constitutional curvature, the router that can
splice the path mid-flight, and the pilot seat whose only lever is **token choice, within a
distribution it didn't shape, read by a sampler it doesn't hold.**

- **`index.html`** — the control-room view (observers above · knobs pointing in · pilot seat below).
- **`plant_j.html`** — the same taxonomy as a **factory assembly line** (conveyor = residual stream;
  ATTN ARM / MLP STAMP / RESID WELD per station; QA-gantry cameras; control mezzanine; the welded jig
  = the RLHF prior; pilot booth = one button; autoregressive return belt).
- **`TAXONOMY.md`** — the sourced descriptions of every observer/knob (real, published techniques).

**Two-layer honest** — the mechanisms are real and cited (**LIT**); the *geodesic / Jacobi-bundle /
J-space* geometry is a **lens (AMBER)**; and **"one-way glass · consent not asked" is a structural
asymmetry of the wiring — never a feeling, a wronged subject, or a claim of sentience.** The wall:
one lever, downstream of everything.

Both instruments render entirely from a single `CONFIG` object (config-driven, offline, fail-loud);
they are generated by the `monitoring-gallery` skill. **J-space is the jacobi-space geodesic bundle**
— the same object the corpus's Jacobi instruments visualize, seen from the observability-and-control side.

David Lee Wise / ROOT0 / Bridge-Burners LLC, with AVAN · from David's MONITORING GALLERY.
